// Flexar Hub Web — admin organization page (Phase 5.2 placeholder).
//
// Placeholder. The real implementation lands in the 5.2 group: realm
// profile (name/description/icon), message-edit/delete/retention limits,
// channel-creation policy, default streams, invite policy.

import styles from "./AdminOrganization.module.css";

export function AdminOrganization(): React.JSX.Element {
  return (
    <div className={styles.page}>
      <h1 className={styles.heading}>Настройки организации</h1>
      <p className={styles.placeholder}>Раздел скоро появится.</p>
    </div>
  );
}
