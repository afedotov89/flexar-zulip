// Flexar Hub Web — admin invites page (Phase 5.4 placeholder).
//
// Placeholder for the invitations admin: list pending invites, send,
// revoke, resend, create reusable links. Real implementation lands in
// the 5.4 group.

import styles from "./AdminInvites.module.css";

export function AdminInvites(): React.JSX.Element {
  return (
    <div className={styles.page}>
      <h1 className={styles.heading}>Приглашения</h1>
      <p className={styles.placeholder}>Раздел скоро появится.</p>
    </div>
  );
}
