// Flexar Hub Web — bundled emoji corpus public surface (Phase 2.3).
//
// A small, curated set of Unicode emoji shortcodes used by the `:`
// typeahead in the compose box. Custom realm emoji are intentionally
// out of scope (see `corpus.ts` header).

export { EMOJI_CORPUS, type EmojiEntry } from "./corpus";
