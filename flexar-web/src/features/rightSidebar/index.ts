// Right sidebar feature — public surface (Phase 1.8).
//
// The app shell mounts `RightSidebar` into its right column. The
// subcomponents, hooks, and pure helpers are feature-internal and not
// re-exported.

export { RightSidebar } from "./RightSidebar";
