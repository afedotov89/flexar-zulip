// Left sidebar feature — public surface (Phase 1.5).
//
// The app shell mounts `LeftSidebar` into its left column. The
// subcomponents and hooks are feature-internal and not re-exported.

export { LeftSidebar } from "./LeftSidebar";
