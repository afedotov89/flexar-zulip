# Zulip backend stand proxied by the Vite dev server (PRD §7.4).
VITE_ZULIP_BACKEND=https://95.84.162.15:8843

# The real .env (gitignored) additionally holds test-account credentials
# used for manual browser testing / Playwright flows. Do not commit it.
